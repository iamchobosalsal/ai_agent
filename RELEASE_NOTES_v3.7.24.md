# AI One-touch v3.7.24

GPU를 교체했을 때 새 그래픽카드의 VRAM에 맞는 모델을 자동으로 다시 선택하도록 개선한 초보자용 버전입니다.

## 주요 변경 사항

- 설치와 실행 상태 문구 한글화
- 언어모델 다운로드 진행률·용량·속도·남은 시간 표시
- 다운로드 상태 확인 시각과 움직이는 진행 막대 추가
- LM Studio 초기 설치 및 AMD ROCm·Vulkan 실행 안정성 개선
- Reset Tool과 신규 설치·재설치 동작 보강
- 최신 실제 화면을 반영한 v3.7 사용 가이드 포함
- 이전 실행 때 저장한 GPU 이름·VRAM과 현재 하드웨어 비교
- GPU 또는 VRAM 등급이 달라지면 현재 PC에 맞는 권장 모델로 자동 변경
- 하드웨어가 그대로라면 사용자가 저장한 모델 선택 유지
- 기존에 내려받은 모델은 자동 삭제하지 않고 보존

## 설치 전 확인

- Windows 10/11 64비트
- AMD Radeon 또는 Radeon PRO GPU 권장
- 고성능 모델은 약 16~22GB 다운로드 가능
- 설치 중 런처와 LM Studio를 강제로 종료하지 말 것

## 모델 변경 순서

`AI 끄기 → 모델 선택 → 설정 저장 → AI 켜기`

## GPU 교체 후 확인

- 16GB급 GPU에서는 `Gemma 4 26B A4B IT`가 자동 권장됩니다.
- 32GB급 GPU에서는 `Qwen 3.6 35B-A3B`가 자동 권장됩니다.
- 새 모델이 아직 없다면 **AI 켜기**를 눌렀을 때 최초 다운로드가 시작됩니다.
- 이전 GPU에서 사용하던 모델 파일은 그대로 남아 있으므로 나중에 다시 선택할 수 있습니다.

## 파일 검증

배포 파일과 함께 제공되는 SHA-256 체크섬을 비교해 주세요.
