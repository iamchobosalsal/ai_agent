# v3.6.94 — Hermes 반복 입력·Windows 작업 지연 개선

2026-09-07. 이번 배포는 v3.6 전용이며 v3.7.41과 별도 Team AI 프로그램은 변경하지 않습니다.

## 변경 사항

- LM Studio 모델 로딩에서 꺼져 있던 컨텍스트 체크포인트를 4개로 설정해, 지원 모델에서 반복되는 입력의 중간 상태를 재사용합니다. 로딩 검증도 같은 값으로 맞췄습니다. 모델 종류·추론/출력 예산·대화 길이를 줄여서 속도를 얻는 변경은 아닙니다.
- Hermes의 간단 안내문에서 빠졌던 Windows 로그 분석기와 ZIP 사진 정리기의 실제 경로·호출 방법을 복구했습니다. 특정 질문 문자열에 맞춘 분기나 최종 답변 삭제 규칙은 추가하지 않았습니다.
- Windows 로그 조회에 시간·수량 제한을 직접 적용하고 한글 UTF-8 출력을 유지합니다. 권한 오류를 정상적인 빈 결과와 구분하며, 최근 경고가 많아도 반환된 재부팅 기록이 합치기 단계에서 빠지지 않도록 했습니다.
- 보조 스크립트 실행 명령에만 `-ExecutionPolicy Bypass`를 지정합니다. 시스템 전체 실행 정책을 변경하거나 관리자 권한을 자동으로 높이지 않습니다.
- 사진 정리기의 사용 범위(파일명 날짜 또는 수정일 기준 월별 정리, 원본 ZIP·비미디어 파일 보존, 동명 파일 충돌 처리)를 안내합니다. EXIF 촬영일 판독 기능을 추가한 것은 아닙니다.

## 검증과 한계

- 같은 Qwen 3.6 35B-A3B 모델과 약 1.55만 입력 토큰의 통제 비교에서 후속 질문은 7.028초 → 0.206초, 정정된 값 질문은 7.054초 → 0.224초였습니다. 답과 정정 값은 일치했습니다. 첫 질문은 6.971초 → 7.128초로 비슷했습니다.
- 위 수치는 단일 환경의 반복 입력 시험이지 모든 요청의 평균 속도나 보장치가 아닙니다. 체크포인트에는 추가 메모리 비용이 있고, 첫 입력·긴 출력·대용량 파일 처리 및 다른 모델의 성능은 다를 수 있습니다.
- 실제 Hermes와 같은 모델로 자연어 로그 조회 및 생성한 작은 ZIP의 사진·영상 정리를 실행했고, 표현을 바꾼 요청도 확인했습니다. 한글 경로, 서로 다른 내용의 동명 사진, 원본 ZIP·메모 보존을 해시와 결과 위치로 검증했습니다.
- 로그 수집기의 쿼리 제한·시간 범위·한글·정상 빈 결과·권한 오류·출력 길이·재부팅 기록 보존 검사를 수행했습니다.
- 로그 조회 성공이 PC 전체의 정상 상태를 증명하지는 않습니다. 모델이 일부 경고의 의미나 시스템 상태를 과하게 단정하는 표현은 남아 있습니다. 모든 진단 정확도나 할루시네이션이 해결된 배포는 아닙니다.
- 기존 자연어 RAG와 메신저·스킬 관련 수정은 유지합니다. 이번에 RAG 검색 엔진이나 별도 검수 모델 호출을 추가하지 않았습니다.

## 기존 사용자 업데이트

1. 기존 런처에서 **AI 끄기**를 누르고 종료 완료 후 런처를 닫습니다.
2. 배포 폴더 전체를 압축 해제하고 설치 **BAT**을 관리자 권한으로 실행합니다.
3. 새 런처의 **v3.6.94**를 확인하고 **AI 켜기**를 누릅니다.
4. 모델 서버와 Hermes·메신저 연결 시작 완료를 확인합니다.

BAT 설치 또는 런처 창 재실행만으로 이미 실행 중인 모델·Hermes가 갱신되지는 않을 수 있습니다. 초기화·제거·모델 재다운로드·PC 재부팅은 필요하지 않습니다. 기존 모델·문서·대화·예약·연결 설정은 지우지 마세요.

기존과 같은 자체서명 MSIX입니다. 인증서 지문과 패키지 SHA-256은 동봉 `package-info.json`에서 확인하세요. 서명용 개인키나 사용자 설정은 배포하지 않습니다.
