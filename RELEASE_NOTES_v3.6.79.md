# AI One-touch v3.6.79

Hermes 스킬이 실제 실행 도구를 놓치거나 브라우저 연결을 요구하던 문제를 바로잡은 안정화 배포입니다.

## 주요 변경 사항

- 스킬 설치기가 `SKILL.md`만 읽지 않고 `instruction.md`와 `README.md`까지 함께 분석합니다.
- 실행 명령이 보조 설명 파일에만 있는 스킬을 문서 전용 스킬로 잘못 분류하던 문제를 수정했습니다.
- 이전 버전에서 실행 도구 없이 설치된 스킬은 다시 설치할 때 필요한 런타임을 자동으로 복구합니다.
- 올리브영 재고 검색 스킬은 검증된 `daiso@1.0.10`을 스킬별 로컬 폴더에 고정 설치합니다.
- Hermes가 올리브영 검색을 수행할 때 로컬 CLI를 직접 호출하며 `npx`, 브라우저 원격 디버깅 또는 `curl`로 우회하지 않도록 했습니다.

## 검증

- 스킬 설치 및 복구, Cloud API·Hermes 연동, 시작 상태 복구, 한·영 전환, UTF-8 설치, 진행 로그 스크롤 유지 회귀 테스트를 모두 통과했습니다.
- 실제 Hermes에서 `안양 엔터식스점에서 립밤 재고 확인 좀 해봐` 질문을 두 차례 실행해 로컬 스킬 호출과 재고 응답을 확인했습니다.
- 배포 MSIX와 Reset Tool의 전자서명 상태를 모두 `Valid`로 확인했습니다.
- 배포 패키지 내부 `runtime.zip`에 수정된 스킬 설치기와 고정 CLI 설정이 포함된 것을 확인했습니다.

## 설치

1. 저장소를 ZIP으로 내려받아 압축을 풉니다.
2. `downloads/v3.6.79` 폴더를 엽니다.
3. `README_KO.txt`를 확인합니다.
4. `1_Install_AI_OneTouch_Internal.bat`을 관리자 권한으로 실행합니다.

기존 설치 위에 설치하면 설정을 유지하면서 런처와 스킬 설치기만 갱신됩니다. 스킬 실행 도구가 빠져 있었던 경우 해당 스킬을 다시 설치해 복구할 수 있습니다.

## 무결성 정보

- MSIX SHA-256: `BA2183D397CC7371022A367C8312915AC405F5DCAAFE5F33C0542A8C901BD8CE`
- Reset Tool SHA-256: `9F5022B5E3E92B89A8E09CA2104BFF5E716666828FB4FCDAA44CBD307D201022`
- 배포 ZIP SHA-256: `2DEA757B8DB9E3E3363D554EF3B575C141F620267E75BD18E12A833EED264D34`
- 서명 인증서 지문: `8CC6F5032FF17B38DA3D4676916ECE159DFCF24C`
- 서명용 개인키는 배포 파일에 포함되어 있지 않습니다.
