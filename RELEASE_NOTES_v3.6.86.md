# AI One-touch v3.6.86

2026-09-05 · Windows Hermes 브라우저 입출력 호환성 수정

## 변경 사항

로컬에서 검증한 v3.6.85에 다음 호환성 수정을 추가했습니다.

- browser-use 자식 프로세스의 표준 입출력을 UTF-8로 지정하고 부모의 텍스트 파이프 인코딩을 일치시킵니다.
- 설치·설정, Hermes Desktop 실행, 메신저 Gateway 시작 시 적용합니다. Hermes 업데이트로 동일 문제가 재발하면 다음 시작 때 다시 적용합니다.
- 원본 백업과 중복 적용 방지를 포함합니다. 이미 수정된 파일은 다시 쓰지 않으며, 지원하지 않는 새 코드 구조는 덮어쓰지 않고 경고합니다.
- 모델, 컨텍스트, RAG, 답변 지침, 도구 재시도 횟수, 브라우저 권한 및 메신저 어댑터는 이번 수정으로 변경하지 않습니다.
- Team AI 별도 프로그램은 이번 배포에 포함되지 않습니다.

## 검증

- 새 패치 설치 도구 단위 검사 11개 및 설치/재실행 경로 검사 통과.
- 메신저·Hermes·서명 관련 기존 회귀 검사 8개 통과.
- 런처가 생성한 패치 파일이 실제 Chrome/browser-use로 검증한 후보 파일과 SHA-256까지 일치.
- 실제 서명 EXE 안의 런타임과 배포 파일 해시 388개 확인. 격리된 화면 서버의 버전 및 HTML 응답 확인.
- 기존 브라우저 실증: Unicode 회귀 검사 15개와 실제 브라우저 호출 9개 성공. 전체 Hermes 테스트 통과나 모든 질문의 속도/정확성을 보장하는 의미는 아닙니다.
- MSIX·내장 EXE·Reset Tool: 동일 게시자 서명 Valid, 타임스탬프 확인.
- 개인 설정, 토큰, 문서 DB와 서명 개인키는 포함하지 않습니다.

배포 MSIX SHA-256: `854B17AF9A8871C15BF0FE335E98E2B91476013473D5DE08AAA7D360D24115CF` (`package-info.json`과 비교)

공개 인증서 지문: `8CC6F5032FF17B38DA3D4676916ECE159DFCF24C`

## 설치 / 업데이트

1. 저장소의 **Code → Download ZIP**으로 내려받고 압축을 완전히 풉니다.
2. `downloads/v3.6.86`의 `1_Install_AI_OneTouch_Internal.bat`을 관리자 권한으로 실행합니다.
3. 기존 런처 창을 닫고 새 런처에서 **AI 끄기 → AI 켜기**를 한 번 실행합니다. PC 재부팅은 필요 없습니다.
4. 기존 모델·메신저 연결 정보는 초기화하지 마세요. 상세 절차는 폴더의 HTML 사용 가이드를 확인하세요.

이 배포는 공인 코드서명이 아닌 기존 방식의 자체서명 MSIX입니다. 최초 설치 시 동봉된 공개 인증서를 신뢰해야 하며, 조직 보안 정책이 금지하는 경우 관리자에게 문의하세요. 보안 정책을 끄는 설치 과정은 포함하지 않습니다.
