# AI One-touch v3.7.43 — Hermes 이미지 작업 호환성

## 수정 내용

- 이전 실패 대화에 남아 있는 도구 이미지도 전송 시점에 호환 처리합니다. 같은 원본이 앞선 사용자 메시지에 있으면 중복 분석 없이 해당 이미지를 유지하고, 도구에만 있는 고유 이미지는 기존 이미지 분석 경로를 사용합니다. 저장된 대화를 지우거나 가짜 사용자 메시지를 추가하지 않습니다.
- Hermes의 이미지 입력 지원과 도구 결과 이미지 지원을 분리했습니다. LM Studio처럼 도구 결과의 이미지 배열을 받지 않는 서버에서는 기존 보조 이미지 분석 경로를 사용합니다. 이미지는 사용자 입력 형식으로 같은 설정의 이미지 지원 모델에 전달하고, 도구에는 분석 텍스트를 돌려줍니다. 잘못된 메시지 형식 오류를 컨텍스트 초과로 오인해 대화를 압축·재시도하던 문제도 수정했습니다.
- 지원되지 않는 도구 메시지 형식의 재시도·대화 압축을 피합니다. 정상적인 컨텍스트 초과 처리는 유지합니다.
- 3.6과 3.7에 동일한 호환성 패치를 포함합니다. 기존 Hermes 파일은 백업하고, 이미 적용됐으면 다시 수정하지 않습니다. 예상과 다른 Hermes 구조에서는 경고를 남기고 변경하지 않습니다.
- 모델 선택·추론/출력 예산·RAG·메신저 대화·예약을 바꾸지 않습니다. Team AI는 포함하지 않습니다.

## 검증과 한계

- 일반 응답뿐 아니라 도구 반복 한도에 도달한 마지막 요약 요청에도 같은 호환 처리를 적용했습니다.
- 실패했던 기존 대화를 이어서, 별도 검증 폴더에 연·월 시트를 가진 XLSX와 2010/03 이미지 복사본 생성까지 확인했습니다(76.4초). 모델이 작성한 코드 오류 2회를 복구한 시간이 포함되며, 파일 생성 성공이 모든 셀·OCR의 정확성을 의미하지 않습니다. 실제 D 드라이브의 사용자 파일은 변경하지 않았습니다.
- 같은 로컬 모델에서 도구 결과 이미지 배열의 HTTP 400을 재현했습니다. 수정한 이미지 분석→도구 텍스트→모델 응답 경로와 표현을 바꾼 이미지→파일 생성 요청을 검증했습니다.
- Hermes 회귀 테스트 202개 통과. 실제 실패 요청의 기존 대화 39개 메시지를 재생해 수정 전 HTTP 400, 보완 후 정상 HTTP 응답을 확인했습니다. 사용자 이미지와 대화는 보존되고 중복 이미지 처리에는 추가 모델 호출이 없습니다. 원본 백업, 중복 적용 및 알 수 없는 코드 구조의 비수정 처리도 검증했습니다.
- 흐린 영수증의 상품명·금액 부호를 잘못 읽는 문제는 남을 수 있습니다. 이번 수정은 통신 호환성 수정이며 OCR/모델 답변의 무오류나 모든 작업의 속도를 보장하지 않습니다.

## 기존 사용자 업데이트

1. 런처에서 **AI 끄기** 후 종료 완료를 확인하고 창을 닫습니다.
2. 새 배포 폴더의 설치 **BAT**을 관리자 권한으로 실행합니다.
3. 런처의 **v3.7.43** 확인 후 **AI 켜기**를 실행하고 메신저 연결 완료를 확인합니다.

BAT 설치나 창 재실행만으로 실행 중인 Hermes가 바뀌지는 않을 수 있습니다. 모델·대화·문서·예약을 지우거나 재다운로드하지 마세요. PC 재부팅은 필요하지 않습니다.
