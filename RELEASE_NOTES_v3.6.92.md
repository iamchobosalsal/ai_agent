# v3.6.92 — 2026-09-06

[설치 파일](downloads/v3.6.92) · [설치·사용 가이드](docs/v3.6/guide.html)

## 메신저 근거 검사와 안내문 개선

- 방문·구매 예정일과 자료의 유효 날짜를 분리했습니다. “내일 근처 점심 추천” 같은 요청이 날짜 검증 실패 문구로 통째로 교체되던 경로를 수정합니다.
- 검색 결과에 실제로 있는 설명도 출처와 함께 활용하며, 정밀 수치·시간별 관측값의 본문 근거와 구분합니다.
- 독립된 항목·필드의 오류를 제한하고 근거가 있는 나머지 설명은 유지하도록 보완했습니다. 표·합계를 임의로 부분 재작성하지 않으며 오류 항목에 의존하는 요약도 검사합니다.
- 조회 실패를 대상의 부재나 영업 가능이라는 추측으로 바꾸지 않도록 보완했습니다. 날짜·요일·출처·수치 검사는 유지합니다.
- 실제 로컬 모델 대조 검사에서 누적 안내문이 응답 미완성에 영향을 줄 수 있어 중복을 줄였습니다. 안내문 크기 제한 검사도 추가했습니다.

## 범위와 한계

Telegram·Discord·Slack 공통 수정입니다. 추가 검수 모델·검색·재임베딩 호출은 없으며 모델 응답 시간에는 조회·생성 비용과 편차가 남습니다. 기존 자연어 `/rag` 개선을 포함하지만 이번 변경은 `/rag` 엔진, 모델 설정, 별도 Team AI를 변경하지 않습니다.

모든 문장의 진위를 판별하는 기능은 아닙니다. 실제 모델 시험에서 상호·메뉴명 혼동이나 출처에 없는 속성 추가 등 의미상 오류도 관찰됐습니다. 검사 통과를 모든 사실이 검증됐다는 보증으로 해석하지 마세요. 중요한 수치와 조건은 원문을 확인해야 합니다.

## 검증과 업데이트

- 근거 검사 98개, 예약 검사 30개 통과. 기존 검색 범위 검사 5개 통과, 사용자 원본 파일을 요구하는 선택 검사 1개는 미지정으로 건너뛰었습니다.
- 실제 로컬 모델의 추천·장비 비교·날짜 사례와 격리 Hermes 처리 경로를 검사했습니다. 사용자 메신저로 테스트 메시지를 발송한 시험은 아닙니다.
- MSIX·EXE·Reset Tool 서명 및 타임스탬프, 실제 EXE 내장 런타임과 수정 코드 해시를 확인했습니다. 서명은 공인 코드서명이 아닌 동봉 공개 인증서 기반 자체서명입니다.

설치 BAT를 관리자 권한으로 실행한 뒤 런처를 다시 열고 **AI 끄기 → AI 켜기**를 한 번 실행하세요. PC 재부팅이나 모델·문서·연결 설정 초기화는 필요 없습니다. 기존 예약과 대화도 초기화하지 않습니다.

`downloads`에는 계열별 최신 배포판 하나씩 유지합니다. 이전 버전은 Git 이력에서 확인할 수 있으며 Team AI 설치 파일은 포함하지 않습니다.
