# v3.6.90 — 2026-09-06

[설치 파일](downloads/v3.6.90) · [설치·사용 가이드](docs/v3.6/guide.html)

## 자연어 RAG 근거 보완

- 공통 `/rag`에서 검색된 문서의 인접 내용과 절을 제한적으로 보충합니다.
- 등록 XLSX에 품목·재고·분류 열이 있으면 원본 행을 읽어 전체 일치 로트와 분류별 구성 후보를 제공합니다. 특정 CPU/GPU 목록으로 질문을 치환하지 않습니다.
- 조건형 구성·추천에서도 후보 탐색을 수행하되, 후보 발견과 모든 조건의 충족을 구분합니다. 미조회·근거 부족을 재고 0이나 부재로 단정하지 않도록 범위를 표시합니다.
- 예시 견적·템플릿의 마진·선택 품목·가정을 현재 질문의 상시 업무 규칙으로 자동 적용하지 않도록 안내합니다.
- 여러 항목의 구성·절차에서 일부 속성 일치를 전체 호환성·자격 검증으로 취급하지 않도록 안내합니다.

## 성능·범위

추가 모델 호출·재임베딩 없이 원본 행 보충은 32,000자, 인접 절은 최대 6개/12,000자 범위로 제한합니다. 읽기와 추가 문맥 처리 비용은 있으므로 모든 질문의 속도가 같다고 보장하지는 않습니다. 원본 누락, 모호한 규칙, 모델의 잘못된 추론까지 완전히 제거하는 기능은 아닙니다.

별도 Team AI의 전용 근거 보충과 중복 실행하지 않습니다. Team AI 설치 파일은 포함하지 않으며 일반 대화, 모델·메신저 설정과 기존 문서는 유지합니다. 기존 예약 알림·날짜 및 근거 검증 수정도 포함합니다.

## 검증·설치

- 실제 서명 EXE 안의 런타임·RAG 모듈과 소스 해시 일치, MSIX·EXE·Reset Tool 서명 및 타임스탬프 확인.
- 원본 표/문서 범위 회귀 검사 6개 통과(테스트용 원본 XLSX 포함). 기존 예약 30개·근거 검증 57개 빌드 검사 통과.
- 격리된 로컬 모델의 조건형 구성·비PC 정책 질문 확인. 실제 메신저로 테스트 메시지를 발송한 검증은 아닙니다.

업데이트 후 런처를 다시 열고 **AI 끄기 → AI 켜기**를 한 번 실행하세요. PC 재부팅이나 데이터 초기화는 필요 없습니다. 설치 BAT는 기존 방식대로 관리자 권한으로 실행합니다. 서명은 공인 인증서가 아닌 동봉된 공개 인증서를 사용하는 자체서명입니다.

`downloads`에는 계열별 최신 배포판 하나씩만 남깁니다. 구버전은 Git 이력에서 확인할 수 있습니다.
